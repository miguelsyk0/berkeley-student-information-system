specVersion: "v1"
serviceId: "berkeley-student-information-system"
location: "asia-southeast1"
schema:
    source: "./schema"
    datasource:
      postgresql:
        database: "fdcdb"
        cloudSql:
          instanceId: "berkeley-student-information-system-fdc"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.

connectorDirs: ["connectors/berkeley"]